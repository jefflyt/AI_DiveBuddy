# Step 1.1: Initialize Next.js Project

**Part of:** PR 1.1 - Next.js Project Setup & UI Framework  
**Focus:** Create Next.js 14 project with TypeScript and essential dependencies  
**Estimated Time:** 10-15 minutes

## Overview

Initialize a new Next.js 14 project using the App Router with TypeScript, ESLint, and Tailwind CSS configured. This establishes the foundation for the entire DiveBuddy application.

## Pre-Step Checklist

- [ ] **Node.js:** Version 18.17 or higher installed (`node --version`)
- [ ] **Git:** Repository initialized at `/Users/jefflee/SCTP/AI_DiveBuddy`
- [ ] **Directory:** Currently in project root directory

## Implementation

### Goal
Create a new Next.js 14 project with TypeScript, Tailwind CSS, and App Router enabled.

### Step-by-Step Instructions

#### Step 1: Create Next.js Project

Run the following command in your terminal:

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
```

When prompted:
- **Would you like to use TypeScript?** Yes
- **Would you like to use ESLint?** Yes
- **Would you like to use Tailwind CSS?** Yes
- **Would you like to use `src/` directory?** Yes
- **Would you like to use App Router?** Yes
- **Would you like to customize the default import alias?** Yes (@/*)

#### Step 2: Verify Project Structure

- [ ] Confirm the following directories were created:
  - `src/app/` - App Router pages and layouts
  - `src/components/` - React components
  - `public/` - Static assets
  - `.next/` - Build output (after first run)

#### Step 3: Install Additional Dependencies

Install shadcn/ui CLI and necessary dependencies:

```bash
npm install class-variance-authority clsx tailwind-merge lucide-react
npm install -D @types/node @types/react @types/react-dom
```

## Verification Checklist

- [ ] `package.json` exists with Next.js 14+ and TypeScript dependencies
- [ ] `tsconfig.json` exists with proper configuration
- [ ] `tailwind.config.ts` exists
- [ ] `src/app/layout.tsx` exists (root layout)
- [ ] `src/app/page.tsx` exists (home page)
- [ ] Run `npm run dev` successfully starts development server at http://localhost:3000
- [ ] Visit http://localhost:3000 and see default Next.js page

## Troubleshooting

| Issue | Solution |
|-------|----------|
| `create-next-app` not found | Run `npm install -g create-next-app` or use `npx create-next-app@latest` |
| TypeScript errors on first run | Run `npm install` to ensure all dependencies are installed |
| Port 3000 already in use | Kill existing process: `lsof -ti:3000 \| xargs kill -9` or use different port: `npm run dev -- -p 3001` |

## Files Created

- ✅ `package.json` - Project dependencies
- ✅ `tsconfig.json` - TypeScript configuration
- ✅ `next.config.js` - Next.js configuration
- ✅ `tailwind.config.ts` - Tailwind CSS configuration
- ✅ `src/app/layout.tsx` - Root layout
- ✅ `src/app/page.tsx` - Home page
- ✅ `.gitignore` - Git ignore rules

## What Comes Next

Next step: **1.2-setup-shadcn-ui.md** - Initialize shadcn/ui component library
